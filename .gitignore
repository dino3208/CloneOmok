코드 스니펫
# 1. 모든 파일 및 폴더 무시
/*

# 2. 필수 Git 설정 파일 유지
!.gitignore
!.gitattributes

# 3. 모든 하위 폴더 탐색 허용 (하위 폴더 내 파일 추적을 위해 필수)
!/**/

# 4. 필수 C/C++ 소스 및 헤더 파일만 예외로 추적
!*.cpp
!*.c
!*.h
!*.hpp

# 5. 프로젝트 문서 (필요한 경우)
!*.md